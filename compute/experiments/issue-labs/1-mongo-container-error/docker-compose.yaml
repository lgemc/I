version: '3.1'
services:
  db:
    image: mongo
    command: --dbpath /data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
    volumes:
      - ./db:/data/db
    ports:
      - 27017:27017
