apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gogs-db-claim
  namespace: gogs
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
